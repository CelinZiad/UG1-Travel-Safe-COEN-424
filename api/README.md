RESTFUL API

This directory contains the code for the RESTful API that serves the area safety scores to end-users. The API is built using FastAPI and is designed to handle requests for retrieving safety scores based on geographical areas and time periods.

### Prerequisites
Before running the API, make sure you run the following commands to install the required dependencies:

```bash
pip install -r requirements.txt
```
